// Environment configuration for production
export const environment = {
  production: true, // Indicates if the app is in production mode
  apiUrl: '/api/search' // Production API URL (e.g., a relative URL)
};

// Environment configuration for development
export const environment = {
  production: false, // Indicates if the app is in production mode
  apiUrl: 'http://localhost:8080/search' // Spring Boot URL for development
};
